## 目标

* 启动本地开发服务，打开首页验证“最新/精选”Tab 按钮切换是否无闪动。

## 执行步骤

* 运行 `pnpm dev` 启动前端（Vite 端口 `5173`）和后端（API 端口 `3010`）。

* 等待启动日志输出本地预览地址（`http://localhost:5173/`）。

* 打开预览链接，实际切换 Tab 验证按钮不抖动。

## 验证点

* Tab 按钮尺寸与边框稳定，仅颜色过渡；点击切换无闪动。

* 列表内容正常加载（非本次验证重点）。

## 不涉及

* 不改动任何代码与配置；仅启动与预览。

