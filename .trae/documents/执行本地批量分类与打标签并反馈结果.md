## 执行步骤
1) 启动本地后端（开发模式）。
2) 访问健康检查确保服务就绪。
3) 管理员登录获取 token（用于受保护接口）。
4) 调用批处理接口 `POST /api/admin/ai/auto-classify`（limit=50, minConfidence=0.6）。
5) 收集并反馈统计结果（total、updated、lowConfidence、errors）。

## 约束
- 不输出或记录任何密钥；仅输出统计结果与必要日志片段。
- 若服务已运行则跳过启动步骤。