# scripts 维护目录

该目录用于存放仅在开发/测试或人工维护场景下执行的 SQL/脚本，不会在生产部署时随迁移自动执行。

建议：
- 仅在确认需求、做好备份后手动运行。
- 明确脚本用途（清理/修复/同步），避免与迁移混用。
- 不要在 CI/CD 流程中调用本目录脚本。

包含示例：
- `clear_skills.sql`：清空技能相关数据（破坏性操作）。
- `sync_usernames.sql`：一次性数据同步/修复。